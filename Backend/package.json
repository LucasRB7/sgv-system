{
  "name": "db-material-objection",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "down": "npx knex migrate:down --knexfile ./db/knexfile.js --migrations-directory ./migrations",
    "migrate": "npx knex migrate:latest --knexfile ./db/knexfile.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "npx knex seed:run --knexfile ./db/knexfile.js",
    "dev" : "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "nodemon": "^3.0.1",
    "objection": "^3.0.1",
    "pg": "^8.11.0"
  }
}
